group("runtime") {
  deps = [
    "//src/hello_world",
    "//src/runtime/runtime_app"
  ]
}

group("default") {
  deps = []
  foreach(t, runtime_toolchains) {
      deps += [
        "//:runtime($t)"
      ]
  }
}