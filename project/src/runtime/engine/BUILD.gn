import("//build/gn/target/runtime.gni")

config("engine_config") {
  include_dirs = [
      "include"
  ]
}

runtime_library("engine") {
  sources = [
    "src/Engine.cpp",
  ]
  all_dependent_configs = [
    ":engine_config"
  ]
}