import("//src/tool/shader_compiler/shader_compiler.gni")

compile_shader("shaders") {
    sources = [
        "default.hlsl",
        "system/system.hlsl",
        "system/post_process.hlsl",
    ]
    output_path = "shaders"
}
